// Reflection (Task 10 - PowerSet)
//
// *4 ---Implementation the Cartesian product of sets.---
//    As part of the implementation of the method, it turned out to use the same algorithm as suggested
//    by the reference solution - two nested loops in which sets are multiplied through the creation
//    of tuples (although the current implementation uses just a string simulating the desired structure).
//    Nevertheless, the best solution would be to implement it through recursion, which supports multiplication
//    of several sets at once.
//    As part of the implementation of the method, it turned out to use the same algorithm as suggested
//    by the reference solution - two nested loops in which sets are multiplied through the creation
//    of tuples (although the current implementation uses just a string simulating the desired structure).
//    Nevertheless, the best solution would be to implement it through recursion, which supports multiplication
//    of several sets at once.
// *6 ---Multi-set Implementation (Bag).---
//    When reassembling the functionality of the Bag class, all the methods had to be reworked, as the logic of adding,
//    deleting, and verifying the presence of an element changed. From the point of view of data storage, an additional
//    array "counters" has been added in which the number of elements is stored. The current solution corresponds
//    to the idea of an eathlon version. It's nice that we managed to implement the "getFrequencies" method quite
//    elegantly in java syntax (using IntStream).

